# AGENTS.md — A-Mao

> 项目宪法：只承载最高约定与文档路由；规则正文住在各专门文档（单一事实源），此处只留指针。修改代码前必须通读并遵守全部红线。未经项目作者同意，严禁修改本文件及任何一级标题。

## 一、项目概述

**A-Mao**：基于 JDK 25 + Spring Boot 4 的 Java 后端微服务脚手架（Vue 前端规划中）。

- **技术栈**：后端 JDK 25、Spring Boot 4.0.8、Spring Cloud 2025.1.0、Spring Cloud Alibaba 2025.1.0.0、MyBatis-Plus 3.5.17、Redisson 4.7.0、Hutool 7.0.0-M7，Maven 多模块单仓；前端规划 Vue 3 + Vite
- **核心理念**：共性下沉 amao-common（统一响应 Result、BaseModel 审计字段、全局异常、链路日志），业务模块只写业务；`*-api` 子模块承载跨模块 RPC DTO 契约；配置与接口契约只增不减

## 二、文档导航与阅读路由

| 何时读 | 文档 |
|--------|------|
| 改任何代码前 | `docs/架构规范与红线.md`（红线/共享层契约/例外登记表）+ `docs/开发规范.md`（编码/Git 规范） |
| 新开分支前 | `docs/开发规范.md` §7（分支规范：命名/生命周期/违反处理） |
| 新设备初始化 / 环境搭建 | `docs/开发环境搭建.md`（前置依赖、中间件启动、构建验证清单）；本机私有配置见 `.env.example` |
| 加/改业务功能 | `docs/踩坑记录.md` 对应模块段落（模块铁律） |
| 查配置字段 / 对外接口 | `docs/配置与接口参考.md`（唯一权威） |
| 目录结构 / 架构分层 | `docs/架构规范与红线.md` §7 |
| 术语 / 决策记录 | `CONTEXT.md`、`docs/adr/` |
| 版本历史 / 待办 | `docs/版本更新记录.md`（历史归档，日常开发无需阅读）；`TODO.md`（完成即删，发布与对话收尾主动核对） |
| 域机制事实 | `docs/域机制.md`（领域机制的唯一权威，按业务域分小节） |
| 建表 DDL / 种子数据 | `sql/`（`DDL.sql`、`init-data.sql`；可执行脚本，非规范正文） |
| 特性级 issue 跟踪 | `.scratch/<feature>/`（约定见 `docs/agents/issue-tracker.md`） |

## 三、最高红线（正文见指针文档，违反即返工）

1. **配置与契约只增不减、即时持久化** → 架构规范 §5。
2. **任务推进禁止无界阻塞**：调度/推进单元每轮只做一个状态判定或一个动作后立即返回；长等待必须节点化挂起；≥0.2s 阻塞等待必须可取消。例外仅限「已批准例外登记表」，新例外必须先落 ADR 再登记 → 架构规范 §2/§3。
3. **跨模块共性判定强制走共享层**（amao-common 对应 starter）；高频/重负载操作强制先收窄数据范围再处理 → 架构规范 §4。
4. **关键动作前置校验、动作后验证生效**；存在语义遮蔽可能时先做负证据检测 → 架构规范 §4.7、开发规范 §3。
5. **日志带 traceId（MDC，amao-common-tracelog）+ userId、关键动作可查、异常现场自落盘** → 开发规范 §2。
6. **发布流程固定顺序**：调整→验证（`mvn test` 全绿）→验收→文档同步→Commit→打包/部署（可选）→ 开发规范 §5 + release 技能；**Git 写操作授权**：非作者明确批准，严禁擅自 commit（**含分支内，每次 commit 须单独批准**）/push/merge/删除分支（见开发规范 §7.1）。
7. **经验沉淀**：验收后按【现象/根因/铁律】写入踩坑记录对应模块 → 开发规范 §6。
8. **禁止 master 直改**：代码与文档改动一律新开分支（命名/生命周期见开发规范 §7）；合并后分支**删除权归作者**，Agent 不自动删。
9. **域特有红线**：
   - 接口统一返回 `Result<T>`（amao-common-web），禁止裸 Map/裸实体出 Controller 层 → `docs/配置与接口参考.md`；
   - 接口统一 `POST` + 动词开头 camelCase 路径、入参走请求体，禁止纯名词路径与 `GET`/`PUT`/`DELETE`/`PATCH` → `docs/架构规范与红线.md` §6；
   - 跨模块共性判定/工具一律收编 amao-common 对应 starter，业务模块禁止私有实现 → `docs/架构规范与红线.md` §4.1。

## 四、跨 Agent Skills 共享机制

所有 AI 技能统一存放于 `.agents/skills/`（SKILL.md frontmatter 仅 `name`+`description`），**严禁在各 Agent 专属目录下直接新增/修改技能文件**。`.codebuddy/.claude/.zcode/.opencode/.mimocode` 等的 `skills/` 均为指向它的 junction，由 `.\.agents\setup_skills.ps1` 幂等创建。原生识别 `.agents/skills/` 的 Agent（Codex / Copilot / Gemini CLI 等）无需桥接。

## 五、工作方式约定

1. **不确定点优先确认**：不确定优先与用户沟通，禁止带着假设硬写。
2. **调研与决策分离**：评估类问题先只读不动，用户拍板再实施。
3. **方案先行**：开发前通读相关文档与代码，计划经用户确认后再动手。
4. **收尾沉淀**：验收通过或对话收尾时，主动用 knowledge-distill 技能沉淀知识，并核对 `TODO.md`。
5. **外部输入原样传递**：用户提供的路径/参数/外部标识是唯一事实源，严禁按模式增删改；不确定先询问（详见开发规范 §3）。
6. **中间产物进 `tmp/`**：Agent 开发中间产物一律放项目根 `tmp/<用途>/`，禁止散落仓库；`tmp/` 不入库 → 开发规范 §3。
